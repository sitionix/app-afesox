# API Specifications

All API contracts are registered in `apis/metadata.yml`.

- REST contracts live in `apis/<service>/rest/`.
- Event contracts live in `apis/<service>/event/`.
- `apis/metadata.yml` is the single registry used by generation workflows.
