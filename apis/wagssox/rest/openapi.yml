openapi: "3.0.0"
info:
  title: API Workspace Aggregation Service Sitionix
  description: API specification for workspace projection service
  version: 0.0.6
  contact:
    name: APP Sitionix
servers:
  - description: Localhost (dev only)
    url: "http://localhost:8080/wagssox"
tags:
  - name: Site
paths:
  /api/v1/sites:
    $ref: './paths/v1/sites.yml'
  /api/v1/sites/{siteId}/overview:
    $ref: './paths/v1/sites-overview.yml'
components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: Service-to-service access token (Bearer).
  schemas:
    SiteOverviewDTO:
      $ref: './schemas/v1/site/site-overview-dto.yml#/SiteOverviewDTO'
    WorkspaceSitesPageDTO:
      $ref: './schemas/v1/site/workspace-sites-page-dto.yml#/WorkspaceSitesPageDTO'
    WorkspaceSiteCardDTO:
      $ref: './schemas/v1/site/workspace-site-card-dto.yml#/WorkspaceSiteCardDTO'
    ErrorDTO:
      $ref: '../../common/rest/error/error-dto.yml#/ErrorDTO'
  responses:
    BadRequest:
      $ref: '../../common/rest/error/responses.yml#/BadRequest'
    NotFound:
      $ref: '../../common/rest/error/responses.yml#/NotFound'
    Conflict:
      $ref: '../../common/rest/error/responses.yml#/Conflict'
    Gone:
      $ref: '../../common/rest/error/responses.yml#/Gone'
    InternalServerError:
      $ref: '../../common/rest/error/responses.yml#/InternalServerError'
    Unauthorized:
      $ref: '../../common/rest/error/responses.yml#/Unauthorized'
    Forbidden:
      $ref: '../../common/rest/error/responses.yml#/Forbidden'
    TooManyRequests:
      $ref: '../../common/rest/error/responses.yml#/TooManyRequests'
