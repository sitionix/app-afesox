AgentProjectFlowResponseDTO:
  title: AgentProjectFlowResponseDTO
  type: object
  additionalProperties: false
  required:
    - projectId
    - flowId
    - nodes
    - edges
  properties:
    projectId:
      type: string
      format: uuid
      description: Agent project identifier.
    flowId:
      type: string
      format: uuid
      nullable: true
      description: Saved flow identifier; null when no saved flow exists.
    nodes:
      type: array
      description: Saved flow nodes.
      items:
        $ref: '../../../openapi.yml#/components/schemas/AgentProjectFlowNodeDTO'
    edges:
      type: array
      description: Saved flow edges.
      items:
        $ref: '../../../openapi.yml#/components/schemas/AgentProjectFlowEdgeDTO'
