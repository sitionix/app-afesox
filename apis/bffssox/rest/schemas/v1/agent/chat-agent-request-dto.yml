ChatAgentRequestDTO:
  title: ChatAgentRequestDTO
  type: object
  additionalProperties: false
  required:
    - message
  properties:
    message:
      type: string
      minLength: 1
      pattern: ".*\\S.*"
      description: User message for one chat execution.
      example: Explain clean architecture in simple words.
