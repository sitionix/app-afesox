AcceptAgentRuleRequestDTO:
  title: AcceptAgentRuleRequestDTO
  type: object
  additionalProperties: false
  properties:
    title:
      type: string
      minLength: 1
      pattern: ".*\\S.*"
      description: Updated rule title before activation.
      example: Language preference
    content:
      type: string
      minLength: 1
      pattern: ".*\\S.*"
      description: Updated rule content before activation.
      example: Always answer in Ukrainian unless user explicitly asks otherwise.
