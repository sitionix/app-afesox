get:
  tags:
    - AgentProject
  operationId: getAgentProjectFlow
  summary: Get agent project flow
  description: >
    Returns the saved flow design for one accessible agent project.
    When no saved flow exists, returns an empty flow without mutating database state.
  security:
    - bearerAuth: []
  parameters:
    - in: path
      name: projectId
      required: true
      description: Agent project identifier.
      schema:
        type: string
        format: uuid
  responses:
    '200':
      description: Agent project flow design
      content:
        application/json:
          schema:
            $ref: '../../openapi.yml#/components/schemas/AgentProjectFlowResponseDTO'
    '400':
      $ref: '../../openapi.yml#/components/responses/BadRequest'
    '401':
      $ref: '../../openapi.yml#/components/responses/Unauthorized'
    '404':
      $ref: '../../openapi.yml#/components/responses/NotFound'
    '500':
      $ref: '../../openapi.yml#/components/responses/InternalServerError'
