AgentProjectFlowPaletteResponseDTO:
  title: AgentProjectFlowPaletteResponseDTO
  type: object
  additionalProperties: false
  required:
    - projectId
    - sources
  properties:
    projectId:
      type: string
      format: uuid
      description: Agent project identifier.
    sources:
      type: array
      description: Available flow source entries containing the user source and visible attached project agent sources.
      items:
        $ref: '../../../openapi.yml#/components/schemas/AgentProjectFlowPaletteItemDTO'
