TicketOperatorLaneSummaryDTO:
  type: object
  properties:
    completed:
      type: integer
      format: int64
    inProgress:
      type: integer
      format: int64
    ready:
      type: integer
      format: int64
    notStarted:
      type: integer
      format: int64
    notNeeded:
      type: integer
      format: int64
