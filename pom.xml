<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/POM/4.0.0">
    <modelVersion>4.0.0</modelVersion>

    <groupId>com.afesox</groupId>
    <artifactId>${env.project.artifactId}</artifactId>
    <version>${env.project.version}</version>
    <packaging>jar</packaging>

    <properties>
        <env.project.artifactId>${env.ARTIFACT_ID}</env.project.artifactId>
        <env.project.version>${env.VERSION}</env.project.version>
        <env.artifact.name>${env.NAME}</env.artifact.name>
        <env.event.name>${EVENT_NAME}</env.event.name>
        <env.event.version>${EVENT_VERSION}</env.event.version>
        <env.event.service>${EVENT_SERVICE}</env.event.service>
        <env.event.package>${EVENT_PACKAGE}</env.event.package>
        <env.event.class>${EVENT_CLASS}</env.event.class>
        <env.event.wrapper.package>${EVENT_WRAPPER_PACKAGE}</env.event.wrapper.package>

        <event.name>${env.event.name}</event.name>
        <event.version>${env.event.version}</event.version>
        <event.service>${env.event.service}</event.service>
        <event.package>${env.event.package}</event.package>
        <event.class>${env.event.class}</event.class>
        <event.wrapper.package>${env.event.wrapper.package}</event.wrapper.package>
    </properties>

    <dependencies>
        <!-- Swagger Annotations -->
        <dependency>
            <groupId>io.swagger.core.v3</groupId>
            <artifactId>swagger-annotations</artifactId>
            <version>2.2.10</version>
        </dependency>

        <!-- OpenAPI: Nullable support -->
        <dependency>
            <groupId>org.openapitools</groupId>
            <artifactId>jackson-databind-nullable</artifactId>
            <version>0.2.1</version>
        </dependency>

        <!-- Springdoc OpenAPI UI -->
        <dependency>
            <groupId>org.springdoc</groupId>
            <artifactId>springdoc-openapi-ui</artifactId>
            <version>1.7.0</version>
        </dependency>

        <!-- ✅ JAKARTA: validation, annotation, servlet -->
        <dependency>
            <groupId>jakarta.validation</groupId>
            <artifactId>jakarta.validation-api</artifactId>
            <version>3.0.2</version>
        </dependency>

        <dependency>
            <groupId>jakarta.annotation</groupId>
            <artifactId>jakarta.annotation-api</artifactId>
            <version>2.1.1</version>
        </dependency>

        <dependency>
            <groupId>jakarta.servlet</groupId>
            <artifactId>jakarta.servlet-api</artifactId>
            <version>6.0.0</version>
            <scope>provided</scope>
        </dependency>

        <dependency>
            <groupId>javax.annotation</groupId>
            <artifactId>javax.annotation-api</artifactId>
            <version>1.3.2</version>
            <scope>compile</scope>
        </dependency>

        <dependency>
            <groupId>com.google.code.findbugs</groupId>
            <artifactId>jsr305</artifactId>
            <version>3.0.2</version>
        </dependency>

        <dependency>
            <groupId>org.springframework</groupId>
            <artifactId>spring-web</artifactId>
            <version>6.1.13</version>
        </dependency>

        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <version>1.18.34</version>
            <scope>provided</scope>
        </dependency>

        <!-- OpenAPI Generator CLI -->
<!--        <dependency>-->
<!--            <groupId>org.openapitools</groupId>-->
<!--            <artifactId>openapi-generator-cli</artifactId>-->
<!--            <version>6.6.0</version>-->
<!--            <exclusions>-->
<!--                <exclusion>-->
<!--                    <groupId>org.slf4j</groupId>-->
<!--                    <artifactId>slf4j-simple</artifactId>-->
<!--                </exclusion>-->
<!--            </exclusions>-->
<!--        </dependency>-->
    </dependencies>

    <build>
        <plugins>
            <!-- Deploy Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-deploy-plugin</artifactId>
                <version>2.7</version>
                <configuration>
                    <skipErrorArtifactDeployment>false</skipErrorArtifactDeployment>
                </configuration>
            </plugin>

            <!-- Compiler Plugin -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.11.0</version>
                <configuration>
                    <release>21</release> <!-- Target Java 21 bytecode and APIs -->
                </configuration>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>api-first</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.openapitools</groupId>
                        <artifactId>openapi-generator-maven-plugin</artifactId>
                        <version>6.6.0</version>
                        <executions>
                            <execution>
                                <id>api-first-execution</id>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <phase>generate-sources</phase>
                                <configuration>
                                    <inputSpec>${project.basedir}/apis/${env.artifact.name}/api-first/openapi-rest.yml</inputSpec>
                                    <generatorName>spring</generatorName>
                                    <output>${project.build.directory}/generated-sources</output>
                                    <apiPackage>com.app-afesox.${env.artifact.name}.api-first.api</apiPackage>
                                    <modelPackage>com.app-afesox.${env.artifact.name}.api-first.dto</modelPackage>
                                    <configOptions>
                                        <delegatePattern>true</delegatePattern>
                                        <useTags>true</useTags>
                                        <modelNameSuffix>DTO</modelNameSuffix>
                                        <dateLibrary>java8</dateLibrary>
                                        <useSpringBoot3>true</useSpringBoot3>
                                        <useLombok>true</useLombok>
                                        <additionalModelTypeAnnotations>@lombok.Builder</additionalModelTypeAnnotations>
                                    </configOptions>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>client</id>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.openapitools</groupId>
                        <artifactId>openapi-generator-maven-plugin</artifactId>
                        <version>6.6.0</version>
                        <executions>
                            <execution>
                                <id>client-execution</id>
                                <goals>
                                    <goal>generate</goal>
                                </goals>
                                <phase>generate-sources</phase>
                                <configuration>
                                    <inputSpec>${project.basedir}/apis/${env.artifact.name}/api-first/openapi-rest.yml</inputSpec>
                                    <generatorName>java</generatorName>
                                    <library>resttemplate</library>
                                    <output>${project.build.directory}/generated-sources</output>
                                    <apiPackage>com.app-afesox.${env.artifact.name}.client.api</apiPackage>
                                    <modelPackage>com.app-afesox.${env.artifact.name}.client.dto</modelPackage>
                                    <invokerPackage>com.app-afesox.${env.artifact.name}.client.invoker</invokerPackage>
                                    <configOptions>
                                        <delegatePattern>true</delegatePattern>
                                        <useTags>true</useTags>
                                        <modelNameSuffix>DTO</modelNameSuffix>
                                        <dateLibrary>java8</dateLibrary>
                                        <useSpringBoot3>true</useSpringBoot3>
                                        <useLombok>true</useLombok>
                                        <additionalModelTypeAnnotations>@lombok.Builder @lombok.AllArgsConstructor</additionalModelTypeAnnotations>
                                    </configOptions>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>event-producer</id>
            <dependencies>
                <dependency>
                    <groupId>org.apache.avro</groupId>
                    <artifactId>avro</artifactId>
                    <version>1.11.3</version>
                </dependency>
                <dependency>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                    <version>3.7.0</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.avro</groupId>
                        <artifactId>avro-maven-plugin</artifactId>
                        <version>1.11.3</version>
                        <executions>
                            <execution>
                                <id>generate-avro</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>schema</goal>
                                </goals>
                                <configuration>
                                    <sourceDirectory>${project.basedir}/apis</sourceDirectory>
                                    <outputDirectory>${project.build.directory}/generated-sources/avro</outputDirectory>
                                    <includes>
                                        <include>common/event/*.avsc</include>
                                        <include>${event.service}/event/${event.name}/${event.version}/**/*.avsc</include>
                                    </includes>
                                    <stringType>String</stringType>
                                    <createSetters>false</createSetters>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>generate-producer-wrapper</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/generated-sources/events</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}/src/main/resources/event-templates/producer</directory>
                                            <filtering>true</filtering>
                                            <includes>
                                                <include>EventProducer.java</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>add-event-generated-sources</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>add-source</goal>
                                </goals>
                                <configuration>
                                    <sources>
                                        <source>${project.build.directory}/generated-sources/avro</source>
                                        <source>${project.build.directory}/generated-sources/events</source>
                                    </sources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>event-consumer</id>
            <dependencies>
                <dependency>
                    <groupId>org.apache.avro</groupId>
                    <artifactId>avro</artifactId>
                    <version>1.11.3</version>
                </dependency>
                <dependency>
                    <groupId>org.apache.kafka</groupId>
                    <artifactId>kafka-clients</artifactId>
                    <version>3.7.0</version>
                </dependency>
            </dependencies>
            <build>
                <plugins>
                    <plugin>
                        <groupId>org.apache.avro</groupId>
                        <artifactId>avro-maven-plugin</artifactId>
                        <version>1.11.3</version>
                        <executions>
                            <execution>
                                <id>generate-avro</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>schema</goal>
                                </goals>
                                <configuration>
                                    <sourceDirectory>${project.basedir}/apis</sourceDirectory>
                                    <outputDirectory>${project.build.directory}/generated-sources/avro</outputDirectory>
                                    <includes>
                                        <include>common/event/*.avsc</include>
                                        <include>${event.service}/event/${event.name}/${event.version}/**/*.avsc</include>
                                    </includes>
                                    <stringType>String</stringType>
                                    <createSetters>false</createSetters>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.apache.maven.plugins</groupId>
                        <artifactId>maven-resources-plugin</artifactId>
                        <version>3.3.1</version>
                        <executions>
                            <execution>
                                <id>generate-consumer-wrapper</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>copy-resources</goal>
                                </goals>
                                <configuration>
                                    <outputDirectory>${project.build.directory}/generated-sources/events</outputDirectory>
                                    <resources>
                                        <resource>
                                            <directory>${project.basedir}/src/main/resources/event-templates/consumer</directory>
                                            <filtering>true</filtering>
                                            <includes>
                                                <include>EventConsumer.java</include>
                                            </includes>
                                        </resource>
                                    </resources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>

                    <plugin>
                        <groupId>org.codehaus.mojo</groupId>
                        <artifactId>build-helper-maven-plugin</artifactId>
                        <version>3.5.0</version>
                        <executions>
                            <execution>
                                <id>add-event-generated-sources</id>
                                <phase>generate-sources</phase>
                                <goals>
                                    <goal>add-source</goal>
                                </goals>
                                <configuration>
                                    <sources>
                                        <source>${project.build.directory}/generated-sources/avro</source>
                                        <source>${project.build.directory}/generated-sources/events</source>
                                    </sources>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

    <distributionManagement>
        <repository>
            <id>github</id>
            <name>Api-first Event Sitionix generator</name>
            <url>https://maven.pkg.github.com/sitionix/app-afesox</url>
        </repository>
    </distributionManagement>
</project>
